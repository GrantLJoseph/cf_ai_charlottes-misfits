{
	"name": "my-first-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:frontend": "esbuild frontend/index.ts frontend/admin.ts --bundle --outdir=dist --minify",
		"deploy": "npm run build:frontend && wrangler deploy",
		"dev": "npm run build:frontend && wrangler dev",
		"start": "npm run build:frontend && wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	},
	"dependencies": {
		"bcryptjs": "^3.0.3",
		"pixi.js": "^8.14.3"
	}
}
